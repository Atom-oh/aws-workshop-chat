node_modules/
dist/
build/
cdk.out/
*.log
.env
web/dist/
.dynamodb/

# cdk context cache — leaks the deploy account ID/region; regenerate with `cdk synth`
infra/cdk.context.json

# ad-hoc debug screenshots and Playwright MCP session artifacts, not source
.playwright-mcp/
image.png
image copy.png
Screenshot *.png
*-drag.png

# content-review-agent's own memory files, not app content
.claude/agent-memory/

# exported container image, not source
*.tar.gz
